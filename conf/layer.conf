# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-ptc8-test"
BBFILE_PATTERN_meta-ptc8-test = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-ptc8-test = "6"

LAYERDEPENDS_meta-ptc8-test = "core"
LAYERSERIES_COMPAT_meta-ptc8-test = "kirkstone"
